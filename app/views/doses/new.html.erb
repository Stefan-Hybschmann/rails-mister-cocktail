<h1>test</h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <!-- add image input -->
  <%= f.submit "Submit dose description", class: "btn btn-primary" %>
<% end %>
